/*
const values_1 = ['a','b','c','d','e'];
const values_2 = ['a','bbb','ccc','d','e'];
const values_3 = ['a','b','c','d','e'];

const usingFilter = values_1.filter(function(item){
    return item > 'b';
});

const usingFind = values_2.find(function(item){
    return item.length > 1;
});

console.log('Filter: ', usingFilter);
console.log('Find: ', usingFind);

const usingForEach = values_3.forEach(function(item){
    return item;
});
*/

/*
const  button = document.getElementById('see-review');

button.addEventListener('click',() =>{
    const review = document.getElementById('review'); 
    if(review.classList.contains('d-none')){
        review.classList.remove('d-none');
        button.textContent = 'Close Review';
    } else {
        review.classList.add('d-none');
        button.textContent = 'See Review';
    }
}); 
*/